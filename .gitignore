# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# uv
.uv/

# The built dataset is committed: it is what the app serves, it takes ten
# minutes and several hundred queries to rebuild, and a deploy has no way to
# run the fetch. The checkpoint and logs behind it are not.
data/_checkpoint.json
data/*.log
data/countries.json.bak

# Local progress (your scores and spaced-repetition schedule)
data/progress.db

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Cached source dumps, re-downloaded on demand by the build scripts
data/cities15000.zip
data/factbook.zip
