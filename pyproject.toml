[project]
name = "learngeo"
version = "0.1.0"
description = "A world-knowledge quiz show: flags, borders, maps, leaders and famous faces, with a fact card after every answer."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1",
    "requests>=2.32",
    "shapely>=2.1.2",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
